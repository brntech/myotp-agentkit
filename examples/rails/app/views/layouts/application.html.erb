<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= content_for?(:title) ? yield(:title) : "Phone verification" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", media: "all" %>
  </head>
  <body>
    <main>
      <h1><%= content_for?(:heading) ? yield(:heading) : "Phone verification" %></h1>
      <%= yield %>
    </main>
  </body>
</html>
