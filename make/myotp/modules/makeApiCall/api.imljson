{
  "url": "/{{replace(replace(trim(parameters.url), '/^[a-z]+:\\/\\/[^\\/]*/i', ''), '/^\\/+/', '')}}",
  "method": "{{parameters.method}}",
  "headers": {
    "{{...}}": "{{toCollection(parameters.headers, 'key', 'value')}}"
  },
  "qs": "{{toCollection(parameters.qs, 'key', 'value')}}",
  "body": "{{parameters.body}}",
  "response": {
    "output": {
      "body": "{{body}}",
      "headers": "{{headers}}",
      "statusCode": "{{statusCode}}"
    }
  }
}
