{
  "name": "@myotp/make-app",
  "version": "0.1.0",
  "private": true,
  "description": "Validates the MyOTP.App Make.com custom app definition in make/myotp against openapi-reference.yaml.",
  "license": "MIT",
  "author": "BroadNet Technologies <info@myotp.app>",
  "type": "module",
  "scripts": {
    "build": "node validate.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  }
}
